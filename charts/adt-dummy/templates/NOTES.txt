adt-dummy is installed.

This chart deploys a toolbox pod used via kubectl exec.

Useful commands:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "adt-dummy.name" . }}
  kubectl exec -n {{ .Release.Namespace }} -it <pod-name> -- dami doctor
  kubectl exec -n {{ .Release.Namespace }} -it <pod-name> -- dami shell

Environment variables are provided via the secret:
  {{ .Values.envFromSecret | default "(not set)" }}
